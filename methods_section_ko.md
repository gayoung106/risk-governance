# 연구방법

## 3. 연구설계와 자료

본 연구는 재난 상황에서 개인정보 활용 정책에 대한 일반 시민의 수용성이 어떤 인식 요인과 연관되는지를 분석한다. 분석 단위는 개인 응답자이며, 자료는 한국 일반 국민 표본 1,094명으로 구성된 횡단면 설문자료이다. 본 연구의 추론 범위는 개인 수준의 인식 연관성에 한정되며, 거버넌스 시스템의 객관적 성과나 제도 구조 자체를 직접 측정하는 것은 아니다.

## 3.1 변수 측정

| 구성개념 | 변수명 | 측정 문항 | 코딩 및 구성 | 해석 방향 |
|---|---|---|---|---|
| 정책수용성 | `consent` | q7, q8, q9 | 3문항 평균, α=.876 | 높을수록 개인정보 활용 정책 수용성 높음 |
| 관리신뢰 | `manage_trust` | q5, q6 | 2문항 평균, α=.834 | 높을수록 기관의 데이터 활용 필요성·효과성 신뢰 높음 |
| 위험인식 | `risk` | q21, q22 | 2문항 평균, α=.833 | 높을수록 개인정보 유출·오남용 위험 인식 높음 |
| 안전관리 긍정 인식 | `safe_management` | q25 | 이분 더미, 1=안전하게 관리된다고 인식, 0=그렇지 않음 | 높을수록 안전관리 긍정 인식 높음 |
| 제도적 신뢰 | `trust` | q26 | 단일 문항 | 높을수록 재난 대응 정부기관 신뢰 높음 |

## 3.2 q25 코딩 방향과 재코딩

q25는 “재난 대응 정부기관에 의해 수집된 귀하의 개인정보가 안전하게 관리되고 있다고 생각하십니까?”라는 문항이다. 원자료의 코딩은 `1=예`, `2=아니오`이다. 따라서 원코딩 q25는 값이 커질수록 안전관리 긍정 인식이 높아지는 변수가 아니라, 오히려 “안전하게 관리되고 있지 않다”는 응답 방향을 나타낸다.

해석의 일관성을 위해 본 연구는 q25를 다음과 같이 긍정 방향 더미 변수로 재코딩한다.

```python
safe_management = 1 if q25 == 1 else 0
```

즉 `safe_management=1`은 응답자가 재난 대응 정부기관에 의해 수집된 개인정보가 안전하게 관리되고 있다고 인식함을 의미하며, `safe_management=0`은 그렇지 않음을 의미한다. 기존 원코딩 q25를 그대로 사용했을 때 보고되는 음의 계수는 실질적으로 “안전하지 않다고 생각할수록 정책수용성이 낮다”는 의미였으므로, 본 수정본에서는 계수 방향을 긍정 방향 변수 기준으로 해석한다.

## 3.3 분석 전략

주효과 검증은 정책수용성(`consent`)을 종속변수로 하고, 관리신뢰(`manage_trust`), 위험인식(`risk`), 안전관리 긍정 인식(`safe_management`)을 핵심 예측변수로 포함하는 OLS 회귀분석을 사용하였다. 성별, 연령, 교육, 소득을 통제한 모형을 함께 추정하였고, 이분산성 가능성을 고려하여 HC3 강건 표준오차를 적용한 대안 모형을 제시하였다.

제도적 신뢰의 역할은 부트스트랩 간접 연관 추정으로 탐색하였다. 핵심 관심은 관리신뢰가 제도적 신뢰를 통해 정책수용성과 연결되는 통계적 간접 연관이다. 안전관리 긍정 인식의 간접 연관은 보조적·탐색적 경로로만 제시하며, 인과적 매개 메커니즘으로 해석하지 않는다.

위험인식의 조건부 연관은 관리신뢰와 위험인식의 상호작용항을 포함하여 검토하였다. 이 분석은 위험 조건에 따라 관리신뢰와 정책수용성의 정적 연관 기울기가 달라지는지를 탐색적으로 확인하기 위한 것이다.

## 3.4 기술통계 및 상관관계 해석

q25의 원자료 분포는 `1=예` 515명(47.07%), `2=아니오` 579명(52.93%)이며 결측치는 없다. 이를 `safe_management`로 변환하면 표본의 47.07%가 안전관리 긍정 인식을 가진 집단으로 분류된다.

원코딩 q25와 정책수용성의 상관은 `r=-.313`이지만, 이는 q25 값이 커질수록 “아니오” 방향이기 때문이다. 긍정 방향 변수인 `safe_management` 기준으로는 정책수용성과의 상관이 `r=.313`으로 해석된다. 따라서 본 연구의 결과 해석은 “안전하게 관리된다고 인식할수록 정책수용성이 높다”는 방향으로 제시한다.

## 3.5 방법론적 주의

`safe_management`는 단일 이분 문항에 기반하므로 안전관리 인식의 세부 차원을 충분히 포착하지 못한다. 따라서 이 변수는 안전관리 체계에 대한 종합적 평가라기보다, 개인정보가 안전하게 관리되고 있다고 보는지에 대한 응답자의 이진적 주관 평가로 해석해야 한다. 또한 횡단면 자료의 한계상 관찰된 연관성을 인과효과로 단정하지 않는다.



